import styles from './RecommendCourse.module.css';

const RecommendCourse = () => {
    return (
        <div className={styles.recommendCourse + ' ' + 'container'}>
            <h1>this is the best</h1>
        </div>
    );
}

export default RecommendCourse;